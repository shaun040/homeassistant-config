 # Turns on lights when motion
 alias: "Motion Light Off"
 trigger:
  platform: state
  entity_id: binary_sensor.ecolink_pir_motion_sensor_sensor_4_0
  state: 'off'
 condition:
  condition: sun
  before: sunset
 action:
   service: light.turn_off
   entity_id: group.all_dining_room

